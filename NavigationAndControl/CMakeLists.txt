
############# PRODUCT SELECTION  ################

SET( BUILD_NAVIGATION ON CACHE BOOL "Build pNav")
IF(BUILD_NAVIGATION)
    SET(BASICAPP_DIRS ${BASICAPP_DIRS} MOOSNavLib pNav)
ENDIF(BUILD_NAVIGATION)

SET( BUILD_HELM ON CACHE BOOL "Build pHelm")
IF(BUILD_HELM)
    SET(BASICAPP_DIRS ${BASICAPP_DIRS} MOOSTaskLib pHelm)
ENDIF(BUILD_HELM)

SET( BUILD_IREMOTE ON CACHE BOOL "Build iRemote")
IF(BUILD_IREMOTE)
    SET(BASICAPP_DIRS ${BASICAPP_DIRS} iRemote)
ENDIF(BUILD_IREMOTE)



SUBDIRS(${BASICAPP_DIRS})
