# Version Configuration file for @PROJECTNAME@ library.
set(PACKAGE_VERSION "@PACKAGE_VERSION@")

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @PACKAGE_VERSION_MAJOR@ )
   set(PACKAGE_VERSION_COMPATIBLE 1) 
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @PACKAGE_VERSION_MINOR@ )
     set(PACKAGE_VERSION_EXACT 1)   
   endif("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @PACKAGE_VERSION_MINOR@ )
endif("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @PACKAGE_VERSION_MAJOR@ )